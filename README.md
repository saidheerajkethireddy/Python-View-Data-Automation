# Python-View-Data-Automation
Feeds respondent-level rNPS, pNPS, and cNPS view data CSVs into NPS calculation scripts, then writes computed outputs into mapped cells of a shared Excel workbook — structured so each chart's values land in predefined cell references, ready for ThinkCell macro automation.
